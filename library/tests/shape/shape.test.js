// TEST 1: Passed
// const Shape = require('./shape');

// describe('Shape', () => {
//      test('constructor should set the properties correctly', () => {
//           const shapeColor = 'red';
//           const border = true;
//           const borderColor = 'black';

//           const shape = new Shape(shapeColor, border, borderColor);

//           expect(shape.shapeColor).toEqual(shapeColor);
//           expect(shape.border).toEqual(border);
//           expect(shape.borderColor).toEqual(borderColor);
//      });
// });

// Not sure why the red error